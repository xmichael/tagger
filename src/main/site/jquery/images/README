 - Extracted images from both Datatables and jquery-ui (cupertino theme) 
 - changed reference in jquery-ui-xxx.css from "images/..." to "../images/.." (path is relative to the css file location, NOT the webpage that uses the css) 
 -> To use another theme just copy its images/ui-* here
